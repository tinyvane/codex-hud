# Codex HUD Plugin

This directory is the marketplace artifact for Codex HUD. It contains the
plugin manifest, lifecycle hooks, operator skill, and a self-contained Node.js
runtime generated from the repository source.

Do not edit `dist/cli.js` directly. Run `pnpm build` at the repository root.
